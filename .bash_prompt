# uncomment the following two lines to display monochrome ruby-version@gemset at the beginning of your prompt:
#[[ -s $HOME/.rvm/scripts/rvm ]] && source $HOME/.rvm/scripts/rvm
#PS1="\$(~/.rvm/bin/rvm-prompt) $PS1"

# uncomment the following two lines to display fancy prompt (user@machine:path (git info) ruby-version@gemset):
[[ -s "$HOME/.rvm/contrib/ps1_functions" ]] && source "$HOME/.rvm/contrib/ps1_functions"
ps1_set --prompt "➤"

